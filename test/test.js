var should = require('should');
var assert = require('assert');

describe('the initial test', function(){
  it('should just be a stub waiting for some functionality', function(done){
    should.exist(should);
    should.exist(assert);
    done();
  });
});